import styled from "styled-components";

const Item = styled.li`
    flex: 0 0 calc((100% - 60px) / 3);
    background-color: rgb(200, 150, 62);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
`

export default Item